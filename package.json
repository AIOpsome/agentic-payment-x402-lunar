{
  "name": "agentic-payment-x402-lunar",
  "version": "0.0.0",
  "private": true,
  "description": "Changeset/versioning tooling only — this is a Composer package, not an npm package. Never published to npm.",
  "scripts": {
    "release": "node .github/scripts/tag-release.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7"
  }
}
